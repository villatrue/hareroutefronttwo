import React from 'react'
// import { ReactComponent } from '*.svg';

const Address = (props) => {
    return (
      <div>
        {props.address.address}
      </div>
    )
  }
  
  export default Address